[package]
name = "prov-ebpf"
version = "0.1.0"
edition.workspace = true

[dependencies]
prov-common = { path = "../prov-common" }

aya-ebpf = { workspace = true }
aya-log-ebpf = { workspace = true }

[build-dependencies]
which = { workspace = true }

[[bin]]
name = "prov"
path = "src/main.rs"
